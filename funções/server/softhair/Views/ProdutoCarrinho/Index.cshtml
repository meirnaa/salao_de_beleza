@model IEnumerable<softhair.Models.ProdutoCarrinho>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Produto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Carrinho)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Produto.ProdutoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Carrinho.CarrinhoId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProdutoCarrinhoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProdutoCarrinhoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProdutoCarrinhoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
